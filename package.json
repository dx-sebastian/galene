{
  "name": "galene",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev --port 5177",
    "build": "astro build && node scripts/prune-dist.mjs",
    "preview": "astro preview --port 5177",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test": "playwright test",
    "test:prod": "GALENE_PROD=1 playwright test",
    "interruptor": "node scripts/comprobar-interruptor.mjs",
    "dossier": "node scripts/dossier-clinico.mjs",
    "enlaces": "node scripts/comprobar-enlaces.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "astro": "^7.2.0",
    "leaflet": "^1.9.4"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
